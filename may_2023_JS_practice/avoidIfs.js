/*
if (a === 1) {
  if (c === "Max") {
    alert("OK");
  }
}
Code an if statement that avoids the nesting above by testing for multiple conditions.
*/

if (a === 1 && c === "Max") {
    alert("OK");
}

/*Assign strings to two variables. Code a prompt specifying the first
 * variable as the message and the second variable as the default response.
 * Assign the user's response to a third variable.
 * None of the variables have been declared beforehand.
 */

var ui = "You";
var ux = "Me";
var uiux = prompt(ui, ux);
